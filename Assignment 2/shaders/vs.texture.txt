#version 430 core

layout(location = 0) in vec4 vPosition;
layout(location = 2) in vec2 vTextureIn;

uniform mat4 modelMat;
uniform mat4 viewMat;
uniform mat4 projMat;

out vec2 f_texcoord;

void main()
{
	gl_Position = projMat * viewMat * modelMat * vPosition;		
	f_texcoord = vTextureIn;
}